/**
 * This package is both a whole application/micro-service root and a sub-domain/bounded context root.
 * As an application root, it contains the configuration and the main entry point.
 * As a bounded context, it contains and defines boundaries of a single functional perimeter (domain or sub-domain).
 */
package io.permasoft.archihexa.pretdebd;